// Filter selects elements from an array based on a condition and returns a new array with the selected elements.

const players=[75,64,88,56.73];

const result=players.filter(num=>num>70);
console.log(result)